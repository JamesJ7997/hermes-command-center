# Build and Run Instructions

## Local Development
1. `npm install`
2. `npm run dev`
3. Open `http://localhost:3000`

## Production (Docker)
1. `docker-compose up -d --build`
2. Open `http://localhost:3000`

## Connectivity
Ensure the Hermes Gateway is running on port 8080 for the UI to connect.
