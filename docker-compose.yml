version: '3.8'
services:
  hermes-ui:
    build: .
    container_name: hermes-ui
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080
    restart: always
