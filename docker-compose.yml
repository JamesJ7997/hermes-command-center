version: '3.8'
services:
  hermes-command-center:
    build: .
    container_name: hermes-command-center
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080
    restart: always
